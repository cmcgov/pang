// Pang.cpp : Defines the entry point for the console application.
//

#include "stdafx.h"
#include "Game.h"

int _tmain(int argc, _TCHAR* argv[])
{

//	sf::VideoMode mode(800, 600);
//	sf::RenderWindow window2(mode, "SFML window");
//	sf::RenderWindow* window = new sf::RenderWindow(sf::VideoMode(800, 600), "SFML window");
//	sf::RenderWindow(sf::VideoMode(800, 600), "SFML window");
//	window2.clear();
	Game::Start();

    return 0;
}

